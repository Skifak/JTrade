# Trader Journal

Локальный журнал сделок с уклоном в **ICT / SMC**: вести позиции вручную или подтягивать из **MT5**, следить за риском, собирать статистику и не терять контекст (плейбуки, killzones, bias, дневник дня). Всё работает **на твоей машине** — без облачного аккаунта и без отправки сделок разработчику.

---

## Что внутри

- **Сделки** — открытые и закрытые, pips, %, плавающий P/L по live-ценам (крипта и часть FX через публичные потоки, без API-ключей).
- **Риск и гейт** — лимиты из профиля, дневной стоп, серии, cooldown, чек-лист из заметок, правила выбранного сценария из плейбука; жёсткие блоки и мягкие предупреждения с возможностью осознанно продолжить.
- **Плейбуки** — стратегии, сетапы (plays), killzones, требования по HTF, условия входа; можно опереться на готовую ICT-базу или собрать свою.
- **Статистика** — привычные метрики, equity, heatmap’ы, разрезы по bias и дисциплине.
- **Аналитика** — отдельная вкладка: маяки состояния, дорожная карта и текстовые ориентиры из **локального** корпуса (без нейросетей и без утечки данных наружу).
- **Журнал дня и глоссарий** — дневник по датам, настроение, чек-листы; энциклопедия терминов со своими скриншотами.
- **Темы** — светлая, «бумага», тёмная и неоновая ICT-палитра.
- **Импорт / экспорт** — отчёты MT5 (HTML), полный ZIP с глоссарием и картинками, или только JSON сделок.

Подробности для разработчиков — в [README в корне репозитория](../README.md).

---

## Данные и приватность

Хранилище по умолчанию — **локально** (в браузере или в профиле десктопного окна). Сеть нужна только там, где ты сам её используешь: котировки и справочные курсы с публичных сервисов. **Шифрования базы нет**: кто имеет доступ к твоему ПК — имеет доступ и к данным приложения. Экспорты и чужие файлы импорта открывай осознанно.

---

## Как запустить (кратко)

Нужен **Node.js 18+**. В этой папке:

```bash
npm install
npm run dev
```

Откроется дев-сервер (удобно для live-цен по WebSocket). **Один файл `dist/index.html`** после сборки лучше не открывать «с диска» двойным кликом — для стабильных WS используй `npm run preview`, свой хостинг или **десктоп-сборку Tauri** (зависимости Windows, `npm run tauri:dev` / `tauri:build` — в [корневом README](../README.md)).

---

## Лицензия

Проект для личного использования.
